SVC?=nginx
up:
\tdocker compose -f $(SVC)/docker-compose.yml up -d
down:
\tdocker compose -f $(SVC)/docker-compose.yml down
logs:
\tdocker compose -f $(SVC)/docker-compose.yml logs -f
ps:
\tdocker compose -f $(SVC)/docker-compose.yml ps
